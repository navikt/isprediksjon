apiVersion: "nais.io/v1"
kind: "Alert"
metadata:
  name: isprediksjon-alerts
  namespace: default
  labels:
    team: teamsykefravr
spec:
  receivers:
    slack:
      channel: '#syfo-alarm'
      prependText: '<!here> | '
  alerts:
    - alert: ISPREDIKSJON IS DOWN
      expr: up{app="isprediksjon", job="kubernetes-pods"} == 10
      for: 5m
      description: "Det er ingen pods for app kjørende i namespace og dermed er appen nede"
      action: "Undersøk hvorfor pods er nede og få den kjørende igjen!"
      severity: danger
