apiVersion: "nais.io/v1"
kind: "Alert"
metadata:
  name: isprediksjon-alerts
  namespace: default
  labels:
    team: teamsykefravr
spec:
  receivers:
    slack:
      channel: '#syfo-alarm'
      prependText: '<!here> | '
